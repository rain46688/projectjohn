<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="member">

<select id="selectMember" resultType="map">
	SELECT * FROM MEMBER
</select> 

<select id="selectMemberById" resultType="member" parameterType="member">
	SELECT * FROM MEMBER WHERE MEM_EMAIL=#{mem_email}
</select>

<select id="selectMemberByNick" resultType="member" parameterType="member">
	SELECT * FROM MEMBER WHERE MEM_NICKNAME=#{mem_nickname}
</select>

<select id="selectMemberByPhone" resultType="member" parameterType="member">
	SELECT * FROM MEMBER WHERE TEL=#{tel}
</select>

<insert id="signUpEnd" parameterType="member">
	INSERT INTO MEMBER VALUES(SEQ_MEMBER_NO.NEXTVAL, #{mem_email}, #{mem_pwd}, #{mem_name}, #{mem_nickname}, #{gender}, 
		#{birthday,jdbcType=DATE}, DEFAULT, DEFAULT, DEFAULT, DEFAULT, #{mem_class}, DEFAULT, #{tel})
</insert>

</mapper>